/* components/shared-result/index.wxss - 适度装饰内容页 */

/* ---------- 结果区域 ---------- */
.shared-result { width: 100%; }

/* ---------- 卦象信息 ---------- */
.hexagram-info {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 32rpx;
  margin-bottom: 24rpx;
  background: #FAFAFA;
  border-radius: 16rpx;
}

.hexagram-left { flex: 1; }

.number { 
  font-size: 24rpx; 
  color: #999999; 
  display: block; 
  margin-bottom: 8rpx; 
}

.name { 
  font-size: 48rpx; 
  font-weight: 800; 
  color: #C41E3A; 
  display: block; 
}

.line-name { 
  font-size: 26rpx; 
  color: #666666; 
  margin-top: 8rpx; 
  display: block; 
}

.hexagram-right { 
  display: flex; 
  flex-direction: column; 
  justify-content: center;
  gap: 16rpx;
}

.trigram-item { }

.trigram-label { 
  font-size: 22rpx; 
  color: #BBBBBB; 
  display: block; 
  margin-bottom: 4rpx;
}

.trigram-value { 
  font-size: 28rpx; 
  color: #333333; 
  display: block; 
  font-weight: 500;
}

/* ---------- 选项卡 ---------- */
.tabs {
  display: flex;
  margin-bottom: 24rpx;
  background: #FAFAFA;
  border-radius: 12rpx;
  padding: 8rpx;
}

.tab {
  flex: 1;
  padding: 16rpx;
  text-align: center;
  font-size: 26rpx;
  color: #666666;
  background: transparent;
  font-weight: 500;
  border-radius: 8rpx;
}

.tab.active {
  background: #C41E3A;
  color: #FFFFFF;
  font-weight: 600;
}

.tab-content { }

/* ---------- 详情列表 ---------- */
.hexagram-detail { 
  padding: 0; 
}

.detail-item {
  padding: 20rpx;
  margin-bottom: 12rpx;
  background: #FAFAFA;
  border-radius: 12rpx;
}

.detail-item:last-child {
  border-bottom: none;
}

.detail-label { 
  font-size: 22rpx; 
  color: #999999; 
  margin-right: 8rpx; 
}

.detail-value { 
  font-size: 26rpx; 
  color: #1a1a1a; 
  line-height: 1.6;
  display: block;
  margin-top: 6rpx;
}

.line-block { 
  font-size: 30rpx; 
  font-weight: 700; 
  color: #1a1a1a; 
  display: block; 
  margin-bottom: 8rpx;
}

/* ---------- 两列 ---------- */
.detail-columns { 
  display: flex; 
  gap: 16rpx; 
  margin-top: 16rpx; 
}

.detail-column { 
  flex: 1; 
}

.column-title { 
  font-size: 24rpx; 
  font-weight: 700; 
  color: #C41E3A; 
  display: block; 
  margin-bottom: 16rpx;
}

/* ---------- 解锁 ---------- */
.share-blur {
  padding: 40rpx;
  margin-top: 20rpx;
  background: #FAFAFA;
  border-radius: 16rpx;
  text-align: center;
}

.blur-text { 
  font-size: 30rpx; 
  font-weight: 600; 
  color: #1a1a1a; 
  display: block; 
  margin-bottom: 12rpx; 
}

.blur-hint { 
  font-size: 22rpx; 
  color: #999999; 
  display: block; 
  margin-bottom: 20rpx; 
}

.share-btn {
  background: #C41E3A;
  color: #FFFFFF;
  font-size: 26rpx;
  font-weight: 600;
  padding: 20rpx 48rpx;
  border-radius: 12rpx;
  border: none;
}
.share-btn::after { border: none; }